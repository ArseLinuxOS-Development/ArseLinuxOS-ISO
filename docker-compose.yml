version: '3'
services:
  arselinux-build:
    container_name: arselinux-build
    image: 'arselinux:latest'
    restart: unless-stopped
    privileged: true
    volumes:
      - /home/ledwards/arselinux:/root
