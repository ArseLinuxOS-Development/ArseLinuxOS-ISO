version: '3'
services:
  arselinux-build:
    container_name: arselinux-build
    build: .
    restart: unless-stopped
    privileged: true
    volumes:
      - /home/USER/ArseLinuxOS-ISO:/root/ArseLinuxOS-ISO
